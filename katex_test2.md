# Test 2: does zero-indent break out of list nesting

1. Top level item
   * Sub bullet
     ### A heading inside the sub bullet, content below is FLUSH LEFT (0 indent)

$$
\delta_t = r_t + \gamma V(s_{t+1}) - V(s_t)
$$

## After

Some trailing top-level text.
